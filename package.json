{
  "name": "semaphore-docs-mcp",
  "version": "1.5.0",
  "description": "MCP server for Semaphore UI documentation",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^4.1.4"
  }
}
